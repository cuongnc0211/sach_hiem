
<div class="row gx-5">
  <%= render @books %>
</div>

<% if @pagy.next %>
  <div class="mb-10">
    <a class="text-arrow-icon" href="#!">
      <%= t '.see_more' %>
      <i data-feather="arrow-right"></i>
    </a>
  </div>
<% end %>
